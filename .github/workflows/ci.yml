name: CI

description: Lints, tests, and builds on pull requests and pushes to master.

on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  test:
    uses: AdGuardSoftwareLimited/ext-shared-actions/.github/workflows/test-and-build.yml@master
    with:
      package_file: ecsstree.tgz
